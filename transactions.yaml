actions:
  - method: "clone"
    input:
      driver: "postgres"
      user: "dave"
      password: "football"
      host: "localhost"
      port: "5432"
      database: "test"
    outputs:
      - driver: "sqlite3"
        path: "./postgres3.db"
